# We can know the container id using id()
x = 100
print(id(x))

# Python stores variable as a container where other programming language stores in memory location
y = x
print(id(y))

z = 200
print(id(z))